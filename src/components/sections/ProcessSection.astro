---
// ProcessSection.astro - Proceso de lista de espera y lanzamiento

const launchSteps = [
  {
    id: 'waitlist',
    number: '01',
    title: 'Únete a la Lista',
    subtitle: '2 minutos para asegurar tu lugar',
    description: 'Completa el formulario y asegura tu lugar en la lista de espera. Primeros 100 gimnasios obtienen precios especiales.',
    duration: '2 min',
    icon: 'user-plus',
    color: 'purple',
    details: [
      'Formulario rápido y sencillo',
      'Confirmación inmediata por WhatsApp',
      'Acceso a grupo exclusivo de lanzamiento',
      'Notificaciones de progreso del desarrollo'
    ],
    deliverables: [
      'Lugar asegurado en lista de espera',
      'Precios de lanzamiento garantizados',
      'Acceso a updates exclusivos',
      'Prioridad en beta cerrado'
    ]
  },
  {
    id: 'beta',
    number: '02',
    title: 'Beta Cerrado',
    subtitle: 'Febrero 2025 - Primeros 20 gimnasios',
    description: 'Los primeros gimnasios de la lista acceden al beta cerrado. Tu feedback ayuda a perfeccionar la app.',
    duration: '4 semanas',
    icon: 'settings',
    color: 'blue',
    details: [
      'Acceso exclusivo a versión beta',
      'Configuración personalizada incluida',
      'Soporte directo con el equipo de desarrollo',
      'Influencia directa en características finales'
    ],
    deliverables: [
      'App beta funcionando en tu gimnasio',
      'Feedback incorporado al producto final',
      'Capacitación personalizada',
      'Data migrada y lista para lanzamiento'
    ]
  },
  {
    id: 'launch',
    number: '03',
    title: 'Lanzamiento Oficial',
    subtitle: 'Marzo 2025 - Tu app lista',
    description: 'Lanzamiento oficial con todas las funciones. Tu gimnasio opera con la versión completa de mass-up.',
    duration: 'Marzo 2025',
    icon: 'rocket',
    color: 'green',
    details: [
      'Versión completa con todas las funciones',
      'Todos los miembros usando la app',
      'Pagos automáticos funcionando',
      'Soporte continuo garantizado'
    ],
    deliverables: [
      'Sistema completamente operativo',
      'Todos los miembros onboardeados',
      'Precios de lanzamiento activos',
      'ROI positivo desde mes 1'
    ]
  },
  {
    id: 'growth',
    number: '04',
    title: 'Crecimiento Acelerado',
    subtitle: 'Abril 2025+ - Resultados visibles',
    description: 'Con mass-up funcionando completamente, ves los resultados: más retención, menos administración, más ingresos.',
    duration: 'Continuo',
    icon: 'trending-up',
    color: 'orange',
    details: [
      'Métricas de retención mejoradas',
      'Tiempo administrativo reducido 80%',
      'Ingresos incrementados por mejor retención',
      'Nuevas funciones basadas en tu feedback'
    ],
    deliverables: [
      'ROI comprobado y medible',
      'Gimnasio funcionando con mínimo esfuerzo',
      'Miembros más satisfechos y retenidos',
      'Caso de éxito documentado'
    ]
  }
];

const timeline = {
  enero: 'Lista de Espera Abierta',
  febrero: 'Beta Cerrado (20 gimnasios)',
  marzo: 'Lanzamiento Oficial',
  abril: 'Precios regulares (fin de oferta)'
};

const guarantees = [
  {
    title: 'Sin Riesgos',
    description: 'Lista de espera gratuita, sin compromisos hasta el lanzamiento oficial',
    icon: 'shield'
  },
  {
    title: 'Precios Bloqueados',
    description: 'Mantienes precio de lanzamiento por 12 meses completos',
    icon: 'lock'
  },
  {
    title: 'Acceso Prioritario',
    description: 'Primero en acceder, primero en obtener beneficios',
    icon: 'star'
  }
];
---

<section class="py-20 bg-gradient-to-br from-gray-50 to-blue-50">
  <div class="container mx-auto px-6">
    <!-- Header -->
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold mb-6 text-gray-900">
        Cómo Asegurar tu Lugar en 
        <span class="bg-gradient-to-r from-purple-600 to-blue-600 bg-clip-text text-transparent">
          el Lanzamiento
        </span>
      </h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto mb-8">
        Proceso simple y transparente. Únete a la lista de espera y sé parte del futuro de los gimnasios pequeños en Colombia.
      </p>
      
      <!-- Timeline Overview -->
      <div class="bg-stone-50 rounded-xl p-6 shadow-lg border border-gray-100 max-w-3xl mx-auto">
        <h3 class="text-lg font-bold text-gray-900 mb-4">Cronograma de Lanzamiento 2025</h3>
        <div class="grid grid-cols-4 gap-4 text-center">
          <div>
            <div class="text-sm font-bold text-purple-600">Enero</div>
            <div class="text-xs text-gray-600">Lista Abierta</div>
          </div>
          <div>
            <div class="text-sm font-bold text-blue-600">Febrero</div>
            <div class="text-xs text-gray-600">Beta Cerrado</div>
          </div>
          <div>
            <div class="text-sm font-bold text-green-600">Marzo</div>
            <div class="text-xs text-gray-600">Lanzamiento</div>
          </div>
          <div>
            <div class="text-sm font-bold text-orange-600">Abril+</div>
            <div class="text-xs text-gray-600">Precios regulares</div>
          </div>
        </div>
      </div>
    </div>

    <!-- Launch Steps -->
    <div class="space-y-12 mb-16">
      {launchSteps.map((step, index) => (
        <div class={`launch-step flex flex-col lg:flex-row items-center gap-8 ${index % 2 === 1 ? 'lg:flex-row-reverse' : ''}`}>
          <!-- Content Side -->
          <div class="flex-1 lg:max-w-lg">
            <div class="flex items-center mb-4">
              <div class={`w-12 h-12 bg-gradient-to-br from-${step.color}-500 to-${step.color}-600 text-white rounded-full flex items-center justify-center font-bold text-lg mr-4`}>
                {step.number}
              </div>
              <div>
                <h3 class="text-2xl font-bold text-gray-900">{step.title}</h3>
                <p class="text-lg text-gray-600">{step.subtitle}</p>
              </div>
            </div>
            
            <p class="text-gray-700 mb-6 leading-relaxed">{step.description}</p>
            
            <!-- Duration Badge -->
            <div class={`inline-block bg-${step.color}-100 border border-${step.color}-200 rounded-full px-4 py-2 mb-6`}>
              <span class={`text-${step.color}-800 font-bold text-sm`}>⏱️ {step.id === 'launch' ? 'Fecha:' : 'Duración:'} {step.duration}</span>
            </div>
            
            <!-- Details Accordion -->
            <div class="space-y-4">
              <div class="bg-stone-50 rounded-lg shadow-sm border border-gray-100 overflow-hidden">
                <button 
                  class="launch-accordion-toggle w-full px-6 py-4 text-left flex justify-between items-center hover:bg-gray-50 transition-colors"
                  data-target={`details-${step.id}`}
                >
                  <span class="font-bold text-gray-900">¿Qué incluye esta fase?</span>
                  <svg class="w-5 h-5 text-gray-500 transform transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </button>
                <div id={`details-${step.id}`} class="accordion-content px-6 pb-4 hidden">
                  <ul class="space-y-2">
                    {step.details.map((detail) => (
                      <li class="flex items-start">
                        <svg class="w-4 h-4 text-green-500 mr-2 mt-1 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                        <span class="text-gray-700 text-sm">{detail}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>
              
              <div class="bg-stone-50 rounded-lg shadow-sm border border-gray-100 overflow-hidden">
                <button 
                  class="launch-accordion-toggle w-full px-6 py-4 text-left flex justify-between items-center hover:bg-gray-50 transition-colors"
                  data-target={`deliverables-${step.id}`}
                >
                  <span class="font-bold text-gray-900">¿Qué obtienes?</span>
                  <svg class="w-5 h-5 text-gray-500 transform transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </button>
                <div id={`deliverables-${step.id}`} class="accordion-content px-6 pb-4 hidden">
                  <ul class="space-y-2">
                    {step.deliverables.map((deliverable) => (
                      <li class="flex items-start">
                        <svg class="w-4 h-4 text-blue-500 mr-2 mt-1 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <span class="text-gray-700 text-sm">{deliverable}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>
            </div>
          </div>

          <!-- Visual Side -->
          <div class="flex-1 lg:max-w-md">
            <div class={`bg-gradient-to-br from-${step.color}-500 to-${step.color}-700 rounded-2xl p-8 text-white relative overflow-hidden`}>
              <!-- Background Pattern -->
              <div class="absolute inset-0 opacity-10">
                <div class="absolute top-4 right-4 w-16 h-16 border border-white rounded-full"></div>
                <div class="absolute bottom-4 left-4 w-12 h-12 border border-white rounded-full"></div>
              </div>
              
              <!-- Icon -->
              <div class="relative z-10 text-center">
                <div class="w-20 h-20 bg-stone-50/20 rounded-2xl flex items-center justify-center mx-auto mb-6">
                  {step.icon === 'user-plus' && (
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
                    </svg>
                  )}
                  {step.icon === 'settings' && (
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    </svg>
                  )}
                  {step.icon === 'rocket' && (
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                    </svg>
                  )}
                  {step.icon === 'trending-up' && (
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                    </svg>
                  )}
                </div>
                
                <h4 class="text-xl font-bold mb-2">Paso {step.number}</h4>
                <p class="text-white/80">{step.title}</p>
                
                <!-- Progress indicator -->
                <div class="mt-6">
                  <div class="flex justify-center space-x-2">
                    {Array.from({length: 4}, (_, i) => (
                      <div class={`w-3 h-3 rounded-full ${i <= index ? 'bg-stone-50' : 'bg-stone-50/30'}`}></div>
                    ))}
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Launch Guarantees -->
    <div class="bg-stone-50 rounded-2xl p-8 shadow-lg border border-gray-100">
      <h3 class="text-2xl font-bold text-center text-gray-900 mb-8">Garantías de Lista de Espera</h3>
      
      <div class="grid md:grid-cols-3 gap-6">
        {guarantees.map((guarantee) => (
          <div class="text-center p-6 rounded-xl bg-gray-50 hover:shadow-md transition-shadow">
            <div class="w-16 h-16 bg-gradient-to-br from-green-500 to-emerald-600 rounded-full flex items-center justify-center mx-auto mb-4">
              {guarantee.icon === 'shield' && (
                <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
                </svg>
              )}
              {guarantee.icon === 'lock' && (
                <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                </svg>
              )}
              {guarantee.icon === 'star' && (
                <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.539-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
                </svg>
              )}
            </div>
            <h4 class="text-lg font-bold text-gray-900 mb-2">{guarantee.title}</h4>
            <p class="text-gray-600 text-sm">{guarantee.description}</p>
          </div>
        ))}
      </div>
    </div>

    <!-- CTA -->
    <div class="text-center mt-12">
      <div class="bg-gradient-to-r from-purple-950 to-blue-950 rounded-2xl p-8 text-white">
        <h3 class="text-2xl font-bold mb-4">¿Listo para Unirte a la Revolución?</h3>
        <p class="text-lg mb-6 text-purple-100">
          Únete a la lista de espera y asegura tu lugar entre los primeros gimnasios en transformar completamente su negocio.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="#lista-espera" 
            class="inline-flex items-center justify-center px-8 py-4 bg-gradient-to-r from-orange-500 to-red-500 hover:from-orange-600 hover:to-red-600 text-white rounded-xl font-bold transition-all shadow-lg hover:shadow-xl"
          >
            🚀 Unirme a la Lista de Espera
          </a>
          <a 
            href="https://wa.me/+573181123660?text=Hola%20mass-up,%20quiero%20información%20sobre%20el%20proceso%20de%20lanzamiento%20y%20la%20lista%20de%20espera"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center justify-center px-8 py-4 border-2 border-white text-white hover:bg-stone-50 hover:text-purple-600 rounded-xl font-bold transition-all"
          >
            Preguntar por WhatsApp
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const accordionToggles = document.querySelectorAll('.launch-accordion-toggle') as NodeListOf<HTMLButtonElement>;
    
    accordionToggles.forEach(toggle => {
      toggle.addEventListener('click', function(this: HTMLButtonElement) {
        const targetId = this.dataset.target;
        if (!targetId) return;
        
        const content = document.getElementById(targetId) as HTMLElement | null;
        const arrow = this.querySelector('svg') as SVGElement | null;
        
        // Close all other accordions in the same step
        const currentStep = this.closest('.launch-step') as HTMLElement | null;
        if (!currentStep) return;
        
        const allContents = currentStep.querySelectorAll('.accordion-content') as NodeListOf<HTMLElement>;
        const allArrows = currentStep.querySelectorAll('.launch-accordion-toggle svg') as NodeListOf<SVGElement>;
        
        allContents.forEach((otherContent: HTMLElement) => {
          if (otherContent !== content) {
            otherContent.classList.add('hidden');
          }
        });
        
        allArrows.forEach((otherArrow: SVGElement) => {
          if (otherArrow !== arrow) {
            otherArrow.classList.remove('rotate-180');
          }
        });
        
        // Toggle current accordion
        if (content) {
          content.classList.toggle('hidden');
        }
        if (arrow) {
          arrow.classList.toggle('rotate-180');
        }
      });
    });
  });
</script>